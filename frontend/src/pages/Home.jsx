import { Card } from "@/components/ui/card";

export default function Home() {
  return (
    <Card className="p-6 bg-[var(--bg-card)] border border-[var(--border-subtle)]">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
    </Card>
  );
}
